DROP TABLE USR_USER IF EXISTS;
DROP TABLE USL_USER_LEVEL IF EXISTS;

CREATE TABLE USR_USER (
	USR_ID BIGINT GENERATED BY DEFAULT AS IDENTITY, 
	USR_PASSWORD VARCHAR(255), 
	USR_USERNAME VARCHAR(255), 
	PRIMARY KEY (USR_ID)
);

CREATE TABLE USL_USER_LEVEL (
	USL_ID BIGINT GENERATED BY DEFAULT AS IDENTITY, 
	USL_LEVEL VARCHAR(255), 
	USR_ID BIGINT NOT NULL, 
	PRIMARY KEY (USL_ID)
);

ALTER TABLE USL_USER_LEVEL ADD CONSTRAINT FKDUY6O3SI8FS5U3H6C9SL8AR7K FOREIGN KEY (USR_ID) REFERENCES USR_USER;